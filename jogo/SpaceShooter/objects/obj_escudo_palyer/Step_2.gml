/// @description Insert description here
// You can write your code in this editor



//se alvo for noone se destoy
if(!alvo)
{
	instance_destroy();
}

//vc vai ficar na mesma posiação que o alvo
if(instance_exists(obj_player) && quantidade_escudo > 0)
{
	x = alvo.x;
	y = alvo.y;
	
	
	
}
